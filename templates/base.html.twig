<!DOCTYPE html>
<html leng ="{{app.request.locale}}">
    <head>
        <meta charset="UTF-8">
        
        <title>{% block title %}Hello City!{% endblock %}</title>
        {% block stylesheets %}
        <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
        {% endblock %}
    </head>
    <body class="flex flex-col justify-between items-center min-h-screen">
      <main role ="main" class="mt-6 flex flex-col items-center justify-center">
        {% block body %} {% endblock %}
        </main>
        <footer class="mt-6">
            <p class="text-gray-400"> 
                &copy:Copyright {{'now'| date('Y')}} 
                &middot: <a href="{{ path('app.about')}}" 
                class="text-indigo-500 hover:text-indigo-600 underline">About us</a>
            </p>
        </footer>
       
        {% block javascripts %}{% endblock %}
    </body>
</html>
